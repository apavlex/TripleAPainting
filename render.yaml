# Do not set Build Command to `vite build` alone — `npm run build` also copies dist → tripleapainting.
services:
  - type: web
    name: TripleAPainting
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
