# Build uses npm so postbuild copies ./dist → ./tripleapainting. Publish may be ./dist or ./tripleapainting.
services:
  - type: web
    name: TripleAPainting
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
