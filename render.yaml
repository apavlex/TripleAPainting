# Use `npm run build` (not `vite build` alone) so npm runs `postbuild` and creates ./tripleapainting.
services:
  - type: web
    name: TripleAPainting
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
