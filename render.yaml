# Vite outDir is ./tripleapainting (same as typical Render publish folder for this project).
services:
  - type: web
    name: TripleAPainting
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./tripleapainting
